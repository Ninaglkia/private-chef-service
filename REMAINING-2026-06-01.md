# Remaining Work — Private Chef Site (2026-06-01)

## Stato: quanto manca a "pienamente funzionante"
Il core booking + payment funziona end-to-end (day/weekend/week/custom → Stripe Checkout → webhook → booking `confirmed`), ma l'**auth non e production-reliable**: la sessione muore in silenzio dopo ~1 ora e i nuovi signup via email/Google non riescono nemmeno ad entrare nell'area protetta. In piu l'**admin panel non mostra nessuna prenotazione pagata** (filtro su status morti) e l'owner non ha modo di completare/rimborsare un ordine ne di vedere i lead `quote_requests`. Onestamente: con i fix di codice qui sotto + verifica DB sei "funzionante per uso reale" in ~1 giornata; per il **lancio con pagamenti veri** servono ancora le credenziali Stripe live + dominio email verificato.

## Da fare in CODICE (posso farlo io subito)
Ordinato per severita. Tutto sotto e fixabile da me senza credenziali/decisioni (salvo dove indicato).

| Priorita | Cosa | File | Fix |
|---|---|---|---|
| CRITICAL | Sessione muore dopo ~1h: cookie a 7 giorni ma JWT scade a 1h e non viene mai refreshato | `src/pages/login.astro:135-137` + tutte le pagine protette | Adottare `@supabase/ssr`: `createServerClient` su cookie Astro + `src/middleware.ts` che fa `getUser()` e riscrive i cookie ruotati ad ogni request. Fix minimo immediato: `onAuthStateChange` (TOKEN_REFRESHED/SIGNED_IN) che riscrive entrambi i cookie + helper server che, se `getUser` fallisce, usa `sb-refresh-token` per `refreshSession` prima di redirigere |
| CRITICAL | Email signup non scrive mai il cookie → nuovo utente rimbalzato a /login | `src/pages/signup.astro:121-141` | Centralizzare la scrittura cookie in un unico handler (`@supabase/ssr` o `onAuthStateChange`). Fix immediato: copiare il blocco `maxAge` di `login.astro:134-137` dentro `handlePostAuthRedirect` prima del redirect |
| CRITICAL | Admin panel non mostra MAI le prenotazioni pagate (filtro status morto) | `src/pages/admin/control-panel.astro:55-59` | Cambiare `.in('status', ['searching','assigned','completed'])` → `['confirmed','completed','refunded']` (o togliere `.in()` e ordinare per `created_at`). One-liner |
| CRITICAL | Tutte le mutation del panel girano con anon key client-side, 0 RLS UPDATE policy → no-op silenzioso | `src/pages/admin/control-panel.astro:177,205-266` | Rimuovere le azioni chef/strike (morte nel modello solo-chef). Spostare le mutation reali (complete/refund/status) su endpoint server `src/pages/api/admin/update-booking.ts` che ri-verifica il cookie + owner/admin e usa `SUPABASE_SERVICE_ROLE_KEY` |
| HIGH | Confirmation page dice "refresh shortly" ma non auto-refresha mai → utente bloccato su "Processing" | `src/pages/confirmation.astro:205-209` | Sul ramo `!isPaid`: `<meta http-equiv="refresh" content="4">` o piccolo script che ripolla lo status booking ogni ~3s per ~30s e swappa l'UI a `confirmed`, con fallback "contact us" |
| HIGH | Nessun modo di segnare booking completato/rimborsato — operazioni owner core mancanti | `src/pages/admin/control-panel.astro:82-107` | Sostituire "Assegna Chef" con "Segna come completata" (→ `completed`) e "Annulla/Rimborsa". Il refund chiama un endpoint server → `stripe.refunds.create({payment_intent})` poi status `refunded` (usa STRIPE_SECRET_KEY gia presente, nessuna nuova credenziale in TEST) |
| HIGH | `quote_requests` (lead Abroad & Bespoke) mai mostrati nel panel | `src/pages/admin/control-panel.astro:61-69,109-172` | Aggiungere sezione "Richieste preventivo" che legge via `supabaseAdmin` da `quote_requests` (name/email/phone/city/date/guests/notes/status) + cambio status. Rimuovere sezioni `chef_applications`/`chef_recruitment_applications` |
| HIGH | No callback OAuth; Google sign-in dipende da implicit flow + cookie manuale | `src/pages/login.astro:157-183` | Aggiungere `src/pages/auth/callback.ts` con `@supabase/ssr` + PKCE (`exchangeCodeForSession`), set cookie server-side, redirect. Puntare `redirectTo` Google a quella route (richiede anche allineamento Redirect URLs su Supabase → vedi DB) |
| HIGH | Copy email transazionali ancora vecchio modello ("Weekly Private Chef", "lun-ven", "Scegli il piano") | `src/lib/email.ts:173,177,224,225,230,237` | Sostituire "Weekly Private Chef" → "Private Chef"; usare `PRODUCTS[plan].name` nella conferma; riscrivere il body welcome al modello day/weekend/week; togliere wording "piano/piani" e il `.replace('_',' ')` morto |
| HIGH | Nessun rate limiting sui POST pubblici (request-quote, create-checkout-session) → spam DB/Stripe/Twilio | `src/pages/api/request-quote.ts:6`, `src/pages/api/create-checkout-session.ts` | Rate limit per IP (+email): sliding-window in-memory o Upstash, + honeypot/captcha (Turnstile/hCaptcha) sui form. Codice scrivibile ora; un limiter hosted (Upstash) o WAF Vercel Pro richiede credenziali |
| MEDIUM | Booking TypeScript type stale (plan/status vecchi) maschera i bug a compile-time | `src/lib/supabase.ts:14,23` | `plan: 'day'\|'weekend'\|'week'\|'custom'`; `status: 'pending'\|'confirmed'\|'completed'\|'cancelled'\|'refunded'`. Cosi il compilatore prende il filtro morto del panel |
| MEDIUM | `complete-profile` fa upsert di `profiles.email` (colonna inesistente in ogni migration) | `src/pages/complete-profile.astro:311-318` | Fix code-only a basso rischio: togliere `email` dall'oggetto upsert (l'email vive gia su `auth.users`/`user.email`). In alternativa migration `add column email` (vedi DB) |
| MEDIUM | `end_date` via `toISOString()` puo essere -1 giorno su server non-UTC | `src/pages/api/create-checkout-session.ts:68-74` | Aritmetica pura UTC: `Date.UTC(y,m-1,d)` + `(days-1)` giorni, formattare con `getUTCFullYear/Month/Date`. Mai toccare local time |
| MEDIUM | Cookie `Secure` blocca il login su http://localhost in dev | `src/pages/login.astro:136-137` | `Secure` condizionale (`protocol === 'https:'`), oppure risolto da `@supabase/ssr` |
| MEDIUM | Card "Grocery Shopping" home contraddice "groceries billed separately" | `src/pages/index.astro:187-189` | Riscrivere: "Chef Nino shops for the finest ingredients; groceries are billed separately at cost." |
| MEDIUM | Card admin booking senza nome/email/telefono cliente; `dashboard.astro` ha ancora il ramo `searching`/"finding a chef" | `src/pages/admin/control-panel.astro:88-104`; `src/pages/dashboard.astro:163-196` | Mostrare `customer_name/email/phone`, `total_price` (cent→EUR), `num_guests`, label plan friendly, `stripe_payment_intent`. Togliere ramo `searching` e copy multi-chef dalla dashboard |
| MEDIUM | `vercel.json` vuoto `{}` — nessun security/cache header | `vercel.json:1` | Aggiungere `headers`: HSTS, X-Content-Type-Options:nosniff, Referrer-Policy, Permissions-Policy, X-Frame-Options:DENY; `Cache-Control: immutable` per `/images/*` e `_astro` |
| MEDIUM | Nessuna pagina 404 custom | `src/pages/` | `src/pages/404.astro` con Layout+Header+Footer, messaggio + CTA verso `/` e `/#pricing` |
| LOW | `reset-password` senza guard per recovery session mancante; dopo reset non setta cookie | `src/pages/reset-password.astro:106-118` | On load `getSession()`: se no recovery session, mostra "richiedi nuovo link" e nascondi form. Dopo `updateUser` setta cookie (blocco login) o redirect a /login con success flag |
| LOW | Phone obbligatorio nel gate profilo ma opzionale nel form → WhatsApp cliente puo non partire | `src/pages/booking.astro:130,550-554`; `src/pages/api/stripe-webhook.ts:97` | Prefill phone dal profilo e/o rendere il campo required, oppure fallback al phone profilo in create-checkout-session |
| LOW | `robots.txt` permette crawl di /admin e /dashboard | `public/robots.txt:1` | Aggiungere `Disallow: /admin`, `/dashboard`, `/complete-profile` + `noindex` su quelle pagine |

Nota: `public/og-image.jpg` ora **esiste** (verificato) → quel finding e gia risolto, niente da fare.

## Serve il DATABASE (Supabase di questo progetto — ref zyptbqfldwvbxntwrfqq)
Tutto qui richiede accesso/verifica diretta al progetto live; molte cose il codice le legge/scrive ma **nessuna migration le crea** (confermato: grep su `supabase/migrations/` non trova `onboarding_completed` ne `profiles.email`).

- **`profiles.onboarding_completed` non esiste in nessuna migration** ma e letto/scritto da `login.astro:141`, `signup.astro:128`, `complete-profile.astro:316`. Se manca sul live → loop infinito su /complete-profile e l'upsert va in errore. Fix: `alter table public.profiles add column if not exists onboarding_completed boolean not null default false;` + aggiornare `handle_new_user` a settarlo `false`. Poi **verificare che la colonna sia effettivamente sul live**.
- **`profiles.email` non esiste in nessuna migration** ma `complete-profile.astro:311-318` lo upserta. Decisione DB-vs-code: o `alter table ... add column if not exists email text;` (e verificare che sia landato), o togliere `email` dall'upsert (fix code-only, gia in tabella sopra — preferito).
- **Constraint `status`/`plan` del live da confermare**: il set di migration locali e contraddittorio; solo `20260601_solo_chef_model.sql` ha gli status/plan giusti. Se il live NON ha applicato 20260601, l'UPDATE a `status='confirmed'` del webhook viene **rifiutato** da un vecchio CHECK e le prenotazioni restano `pending` per sempre. Verificare/applicare su live.
- **`handle_new_user` default role**: deve essere `'customer'` e `profiles_role_check` deve permettere `('admin','customer','chef')`. La prima migration usava `'client'/'chef'`: se il vecchio trigger e ancora attivo, i nuovi utenti prendono `role='client'` (fuori dalla TS union). Confermare la definizione attiva sul live.
- **Ruolo owner**: verificare che `profiles.role` di `ninaglia089@gmail.com` sia effettivamente `'admin'` (la migration `20260129_set_admin_role_v2.sql` lo setta ma non confermabile da qui). Diventa irrilevante una volta che le write admin passano da endpoint service-role.
- **RLS troppo larghe (data exposure)**: `quote_requests` SELECT anon `USING (customer_email IS NOT NULL)` e bookings SELECT `USING (stripe_session_id IS NOT NULL)` lasciano leggere a chiunque QUALSIASI riga iterando UUID/session_id. Fix: spostare la lettura confirmation server-side con service-role (la pagina e gia SSR) e restringere le policy anon. Verificare e correggere sul live.

## Servono decisioni di Nino
- **Strategia lingua**: login/signup sono in italiano, il resto del sito in inglese (anche il dropdown header mischia "Il mio profilo/Cliente/Esci"). Scegliere: English-only, Italian-only o i18n. Poi traduco di conseguenza (`src/pages/login.astro:13`, `signup.astro`, `Header.astro`).
- **Copy multi-chef/agency vs single-chef (Chef Nino)**: voce ancora plurale in `index.astro:165,306`, `terms.astro:34,38`, `privacy.astro:44` ("we match you with a chef from our roster"). Confermare il tone of voice singolare desiderato, poi sweep di codice.
- **Recruitment/work-with-us**: la pagina e de-linkata ma raggiungibile via URL e trascina 4 dipendenze pesanti (tfjs, mobilenet, pdfjs, mammoth → chunk lazy da 2.8MB). Decidere se tenerla (lascio com'e, lazy gia ottimale) o ritirarla (cancello `work-with-us.astro`, `notify-recruitment.ts`, `lib/ai-verification.ts` + drop dei 4 pacchetti).
- **Cleanup tabelle/colonne morte**: se "Lavora con noi" resta ritirato, decidere se droppare `chef_applications`, `chef_recruitment_applications`, `bookings.chef_id/chef_payout`, `profiles.strikes` con una migration di follow-up.
- **Favicon**: ora c'e ancora la favicon Astro stock. Vuoi un mark "Private Chef"/monogramma Chef Nino? (asset, poi drop-in).

## Servono credenziali/account (lancio)
- **Stripe LIVE**: oggi e in TEST mode. Per pagamenti veri servono `STRIPE_SECRET_KEY` live (`sk_live_...`), un webhook endpoint **live-mode** registrato su Stripe Dashboard → `https://ninos-privatechefs.com/api/stripe-webhook` (evento `checkout.session.completed`), il relativo `STRIPE_WEBHOOK_SECRET` live, e tutto settato su **Vercel Production** (non solo `.env` locale). Senza webhook secret giusto in prod: nessuna prenotazione viene mai confermata, nessuna email/SMS parte, i clienti restano `pending` per sempre. Confermare anche che l'account Stripe sia attivato per charge live. (`stripe-webhook.ts:7-16`, `create-checkout-session.ts:6-8`)
- **Resend (email)**: `RESEND_API_KEY` su Vercel + verifica dominio `ninos-privatechefs.com` (SPF/DKIM), altrimenti `email.ts` fa no-op silenzioso e le conferme non partono. (`src/lib/email.ts:4-7,34-37`)
- **Twilio/WhatsApp**: oggi usa il **sandbox** `whatsapp:+14155238886` → consegna solo a numeri che hanno fatto join al sandbox, quindi i clienti reali NON ricevono il WhatsApp. Serve un sender WhatsApp Business approvato (con template) oppure fallback SMS, e confermare che `TWILIO_FROM_NUMBER` sia un numero SMS-capable acquistato. (`src/lib/sms.ts:7,12-16`)
- **Error monitoring + analytics**: nessun Sentry/analytics. Serve un account/DSN (Sentry Astro SDK) + `@vercel/analytics` o Plausible. Senza, gli errori prod (Stripe/webhook/email falliti, oggi swallowed con `console.error`) sono invisibili. (`astro.config.mjs`)
- **(Opzionale) Rate-limit hosted / Vercel Pro WAF**: il rate limiting base lo scrivo in codice; un limiter hosted (Upstash) o il WAF di Vercel Pro richiede account/credenziali.

## Ordine consigliato di esecuzione
Code-first: massimizzo cio che posso fare subito, poi gate su DB e credenziali.

1. **Verifica DB live (sblocca tutto il resto)**: controllare su zyptbqfldwvbxntwrfqq che esistano `onboarding_completed`, che i CHECK `status/plan` siano quelli di `20260601` e che `handle_new_user` defaulti a `customer`. Applicare le migration mancanti. Senza questo, i fix auth/payment non danno garanzie.
2. **Fix auth (CRITICAL)**: migrare a `@supabase/ssr` con `middleware.ts` + `auth/callback.ts` PKCE; risolve in un colpo: refresh sessione, cookie su signup, OAuth, e `Secure` su localhost. Aggiornare i Redirect URLs su Supabase.
3. **Fix admin panel (CRITICAL/HIGH)**: correggere il filtro status, creare l'endpoint server `api/admin/update-booking.ts` (service-role + owner gate), aggiungere azioni complete/refund, mostrare i `quote_requests`, rimuovere le sezioni chef. Allineare il `Booking` type in `supabase.ts`.
4. **Fix confirmation auto-refresh (HIGH)** + `end_date` UTC (MEDIUM) + togliere `profiles.email` dall'upsert (code-only).
5. **Restringere RLS** anon su `quote_requests`/`bookings` e spostare la lettura confirmation server-side (service-role).
6. **Rate limiting + honeypot** sui due POST pubblici; `vercel.json` security/cache headers; pagina `404.astro`; `robots.txt` Disallow.
7. **Copy sweep**: email transazionali al modello flat, card "Grocery Shopping", dashboard (togliere `searching`/"finding a chef"). Le parti multi-chef/agency attendono la **decisione lingua/voce di Nino**.
8. **Decisioni di Nino**: lingua, voce single-chef, recruitment keep/retire, favicon. Poi applico in codice.
9. **Lancio (credenziali)**: Stripe live + webhook live su Vercel Production; Resend API key + dominio verificato; WhatsApp Business sender (o fallback SMS); Sentry + analytics. Test event Stripe sull'endpoint live e confermare che un booking passi a `confirmed`.
